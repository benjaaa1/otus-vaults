import React from "react";
import styled from "styled-components";
import { BaseTag } from "../../designSystem";
import colors from "../../designSystem/colors";

export const ProductTag = styled(BaseTag)`
`;

export const AssetTag = styled(BaseTag)`
`;

export const OptionTypeTag = styled(BaseTag)`
`;
